# What does the ^ operator do in Python, and in what context is it commonly used?
# it sets the answer to "true" only if one bit is "true"
# and false if both are set to either "True" or "false"
input1 = False
input2 = False
print(input1 ^ input2)
